{
  "title": "Different Ways To Add Parentheses",
  "difficulty": "Medium",
  "type": "array",
  "tags": [
    "Divide and Conquer",
    "Recursion",
    "Memoization"
  ],
  "description": "Given a string of numbers and operators, return all possible results from computing all the different possible ways to group numbers and operators.",
  "target": "2-1-1",
  "pseudocode": "1. function diffWaysToCompute(input) {\n2.   if (memo.has(input)) return memo.get(input);\n3.   let res = [];\n4.   for (let i = 0; i < input.length; i++) {\n5.     if (isNaN(input[i])) {\n6.       let left = diffWaysToCompute(input.substring(0, i));\n7.       let right = diffWaysToCompute(input.substring(i + 1));\n8.       for (let l of left) for (let r of right) res.push(calculate(l, r, input[i]));\n9.     }\n10.  }\n11.  if (res.length === 0) res.push(parseInt(input));\n12.  return res;\n13. }",
  "steps": [
    {
      "codeLine": 4,
      "note": "Iterate through '2-1-1' to find operators.",
      "i": 1
    },
    {
      "codeLine": 6,
      "note": "Split at index 1 ('-'), recursively calculate left part '2'.",
      "i": 1
    },
    {
      "codeLine": 11,
      "note": "Base case: '2' is a number, return [2].",
      "i": 1
    },
    {
      "codeLine": 7,
      "note": "Recursively calculate right part '1-1'.",
      "i": 1
    },
    {
      "codeLine": 8,
      "note": "Combine left [2] and right [0] using '-' result [2].",
      "i": 1
    },
    {
      "codeLine": 4,
      "note": "Iterate to next operator at index 3.",
      "i": 3
    },
    {
      "codeLine": 6,
      "note": "Split at index 3 ('-'), recursively calculate left part '2-1'.",
      "i": 3
    },
    {
      "codeLine": 8,
      "note": "Combine results of '2-1' and '1', result [0, 2].",
      "i": 3
    }
  ]
}